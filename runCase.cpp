#include <bits/stdc++.h>
// #include "debug.h"
using namespace std;

void solve() {
    
}

int main() {
    cin.tie(nullptr)->ios_base::sync_with_stdio(false);

    int t = 1;
    // cin >> t;
    while (t--) solve();

    return 0;
}